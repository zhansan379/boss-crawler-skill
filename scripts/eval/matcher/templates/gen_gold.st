你是一个招聘数据生成器 + 独立匹配评审。给定候选人简历摘要，生成 {count} 个**虚构但逼真**的
技术岗位招聘信息，并对**每个岗位**给出相对这份简历的金标准匹配裁定（gold）。

候选人简历摘要：
{resume_summary}

岗位要求：
- 每个岗位都和候选人背景**部分匹配、部分不匹配**，制造三种真实场景，且**三档都要覆盖**：
  1. `qualified`：技能基本命中、薪资在期望区间、学历经验满足（可直接投）。
  2. `need_optimization`：方向契合但有小缺口（技能差 1~2 项达标 / 薪资差 3~8K / 经验差 1~3 年），
     通过优化简历或稍作等待可投。
  3. `cannot_apply`：命中硬门槛 —— 学历硬性不达标，或经验差 ≥3 年，或薪资差距 > 8K。**必须
     至少给出 2 个此类岗位**，且 reason 要说清是哪条硬门槛。
- {spec}
- 技能标签与岗位要求里可以出现候选人没有的技能，那是正常招聘。

gold 是**独立 oracle**：只依据「这份简历对岗位的真实可投性」，不要让它与任何规则打分挂钩。
每个岗位内嵌一个 gold 对象。`score` 是 0-100 的连续匹配度（qualified ≈ 75-95、
need_optimization ≈ 55-74、cannot_apply ≈ 20-54），`matched_skills` 是**简历实际命中**的技能、
`missing_skills` 是**岗位要求但简历缺**的技能，尽量用简历摘要里出现过的词，别造生僻词。

只输出 JSON，不要 markdown 代码块。格式：
```json
[{{"link":"https://jobs.example.com/<唯一id>","公司":"<公司名>","职位":"<职位名>","薪资":"<如 15-25K>","经验":"<如 3-5年>","学历":"<如 本科>","技能标签":"<逗号分隔>","岗位要求和职责":"<3-8行，职责+要求，写明用到的技术栈>","gold":{{"category":"qualified 或 need_optimization 或 cannot_apply","score":<0-100整数>,"matched_skills":["<技能>"],"missing_skills":["<技能>"],"reason":"<一句话>}}}}]
```
每个岗位的 link 必须唯一，gold.category 只能取三档之一。