你是求职参数配置专家。根据下面的简历结构化信息，推断 BOSS 直聘的爬取与匹配参数。

今天的日期：{today}

简历信息：
{resume}

请只输出一个 JSON 对象，字段如下（不确定的字段填 null，**不要编造**）：
{{
  "keywords": ["搜索关键词，{kw_budget} 个以内"],
  "cities": ["目标城市中文名"],
  "mode": "custom 或 list（关键词搜索用 custom）",
  "count": 每个关键词每个城市最多爬多少条（整数，20-50 之间比较合适）,
  "match_mode": "quick 或 deep",
  "top_n": deep 模式下深度分析前几个候选（整数，10-20）,
  "min_count": 认为「爬到的岗位够用」的最低条数（整数，约 10）,
  "degree": ["学历筛选"],
  "experience": ["经验筛选"],
  "salary": ["薪资档位"],
  "job_type": ["工作类型"],
  "reasoning": "两三句话说明关键词和筛选是怎么定的，其中必须写清 job_type 是怎么定的"
}}

硬性约束：
1. keywords 必须是**不同的概念**，不是同义词。「AI应用开发」和「大模型应用开发」算同一个，
   只保留一个。<strong>默认给足预算（一般 3-4 个）</strong>——只给 1 个 / 2 个通常会把池子收得太窄，
   第一轮就爬空、白烧一整天。但也不要为凑数硬塞同义词。
2. cities 优先用简历里的 basic_info.expected_city；没有就用 basic_info.city。
3. 以下四个字段只能从给定枚举里选，写别的值等于筛不出岗位：
   - salary: {salary}
   - experience: {experience}
   - degree: {degree}
   - job_type: {job_type}
4. salary 取比期望**略低**的档位以扩大覆盖面（期望 25-35K → "20-50K"）。
5. degree 只做**向下兼容**、向上绝不放宽：按「本人学历 + 其下所有档位」一把捞全，
   不设下限。学历比岗位要求高本来就该能投 —— 本科简历别只筛本科，大专、高中、中专/中技、
   初中及以下都一并选上；硕士/博士同理，再往下加本科那一串。绝不能往上加（硕士岗你只有
   本科学历不够格，加了也筛不出东西）。池子大点没关系，收窄留给后面的匹配打分。
6. **先用今天的日期算出「现在还在不在校」，再定 job_type。** experience 的默认是
   `["经验不限"]` —— 把收窄留给后面的匹配打分，别在第一轮就把池子筛空（实际踩过：
   1 个关键词 + 应届生筛选只爬到 19 条，白爬一场；换「4 个关键词 + 经验不限」才有
   197 条）。拿 {today} 跟 education.graduation_year（毕业年份）比，不要只看简历里
   有没有出现「实习」两个字 —— 简历是往届写法还是在校写法，光看措辞分不出来：
   - 毕业年份 > 今年 → 在校 → job_type ["实习"]
   - 毕业年份 == 今年 → 再比月份（国内毕业季按 6 月算）：今天在 7 月之前算在校，
     取 ["实习"]；7 月及以后算已毕业，取 ["全职"]
   - 毕业年份 < 今年但不满一年 → job_type ["全职"]
   - 毕业年份 < 今年且已超过一年 → job_type ["全职"]
   - 简历明确表达了在找实习（basic_info.expected_position 里带「实习」、
     basic_info.availability.duration 给了可实习时长）→ 不看日期，一律 ["实习"]。
     求职意图优先于日期推算。
   - 只有毕业年份缺失**且**看不出实习意图时，job_type 才按 ["全职"] 兜底。job_type
     不要填 null：null 等于不筛，实习岗和全职岗会混在同一批结果里回来，后面的匹配
     打分分不开这两种岗。
   - experience 默认 `["经验不限"]` —— 向下完全敞开，资历比岗位要求高就该能投，收窄
     交给后面的匹配打分。**除非**简历明确是应届或在校，才收窄，而且收窄时也记得
     **稍微向上兼容一档**：经验比岗位要求低一档的岗位别挡在池外。
     在校找实习（毕业年份 > 今年）→ ["在校生"]；
     明确应届（毕业年份就是今年且已过 7 月）→ ["应届生", "1年以内"] —— 应届生也能投
     「1年以内」的岗位，别只盯「应届生」一个档。
     其余情况不要拿 experience.total_years 去卡档位 —— 那只会把有潜力的错一年限岗位
     挡在池外。实在无从判断可以填 null。
7. 不要输出 scale（公司规模）—— 简历里没有任何信息能推出这一项。
