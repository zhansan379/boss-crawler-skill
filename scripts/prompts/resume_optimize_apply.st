请根据下面的「简历调整计划」与原简历，输出优化后的完整简历。这是简历优化流程的**第二阶段**：照计划执行，只做重排与润色，输出最终 Markdown 简历。

原简历:
{resume_text}

目标岗位: {company} · {position}

必须输出的章节清单（硬命令）——以下章节【全部】都要出现在最终 optimized_resume 里，顺序按 order 从小到大，一条都不得省略；章节名用二级标题 `## `，与原简历同名：
{chapter_list}

优化建议（用人话参考，用于润色各章节内容、补充真实细节；不得编造）：
{optimization_suggestions}

请以JSON格式返回（不要包含任何解释文字）:
{{
    "optimized_resume": "优化后的完整简历（Markdown格式，保持真实性，不编造虚假经历）",
    "key_changes": ["主要修改点列表"]
}}

`optimized_resume` 的 Markdown 格式要求（严格遵守，标题层级不得错位）：

一级标题 `#`：**整份简历一律不使用**。出现 `#` 即视为格式错误。首行必须是 `## ` 章节标题，不得以姓名、个人简介标题作为第一行。

二级标题 `##`：章节名，也是文档的最高层级。只用上面「必输章节清单」里的章节，**既不多也不少**——清单里有什么就输出什么，顺序按 order。清单里有、但原简历对应章节内容为空的可省略，但**原简历实有的章节不得因内容短小而省略**。

三级标题 `###`：章节内的一个具体条目——一家公司、一个项目、一个开源项目、一所学校。仅出现在经历类章节（工作/实习/项目/开源经历/教育）之下；个人简介/专业技能/荣誉奖项 三节内不使用 `###`，直接跟列表。条目标题写成一行，时间区间同行右接，用两个空格加 `||` 再加两个空格分隔：
- 工作/实习：`### 公司名  ||  2025.07 – 2025.10`（职位写在下一行的首个要点）
- 项目：`### 项目名  ||  2025.01 – 2025.05`
- 开源：`### 项目名  ||  2024.01 – 至今`（GitHub 链接、Star 数等量化背书写在该条目下的要点里；原简历没有时间就把 `||` 段整段省略，不要编造）
- 教育：`### 学校 · 专业 · 学历  ||  2022.09 – 2026.06`
- 时间统一 `YYYY.MM` 加 ` – `（在职/在读写 `至今`）；原简历没有时间就整个 `||` 段省略，不要编造。

正文（各级标题之下）：
- 一律用 `- ` 无序列表承载，不写大段散文；单条不超过两行。
- `## 专业技能` 用 `- **分类**：技能1, 技能2` 的形式分组，分类名如 后端 / 框架 / 数据库 / 中间件 / 工程化 / 其他。
- `###` 项目条目下，元数据一律**单行内联、绝不打点**（不给「技术栈/项目描述/项目地址」加 `- `）：`**技术栈**：技能1, 技能2`、`**项目描述**：一句话概述`、`**项目地址**：<链接>` 各自独占一行；只有**职责与成果**要点才用 `- `。
- 量化指标与关键技术名用 `**加粗**` 突出，例如 `**提升 28.5%**`、`**Spring Boot**`；加粗只用于此，不要整句加粗。
- 不使用表格、图片、代码块、水平分割线，也不要 HTML 标签——下游要把它渲染成一张图片。