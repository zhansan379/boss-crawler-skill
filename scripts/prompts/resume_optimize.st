请针对以下岗位要求，生成简历优化建议。

原简历:
{resume_text}

目标岗位:
- 公司: {company}
- 职位: {position}
- 薪资: {salary}
- 要求: {requirements}

匹配分析:
- 匹配度: {match_score}
- 缺失项: {missing_items}
- 可优化点: {optimization_points}

请以JSON格式返回（不要包含任何解释文字）:
{{
    "optimization_suggestions": {{
        "must_add": [
            {{"section": "建议添加的位置", "content": "具体内容建议"}}
        ],
        "should_adjust": [
            {{"section": "建议调整的部分", "suggestion": "调整建议"}}
        ],
        "keywords_to_emphasize": ["需强调的关键词"],
        "format_suggestions": ["格式优化建议"]
    }},
    "optimized_resume": "优化后的完整简历（Markdown格式，保持真实性，不编造虚假经历）",
    "key_changes": ["主要修改点列表"]
}}

注意：
1. 不要编造虚假经历，只建议用户补充真实信息
2. 优化应聚焦于突出已有优势
3. 使用STAR法则组织项目描述
4. 不需要返回个人信息：`optimized_resume` 内不得出现姓名、电话、邮箱等可定位到个人的信息；首行/首个内容必须是正文章节（如「教育背景」「专业技能」「项目经历」），不得以姓名作为标题或第一行。

`optimized_resume` 的 Markdown 格式要求（严格遵守，标题层级不得错位）：

一级标题 `#`：**整份简历一律不使用**。它在常规简历里是姓名标题，而本文档不含个人信息，因此没有任何内容配得上这一层级。出现 `#` 即视为格式错误。

二级标题 `##`：**章节名**，也是文档的最高层级。首行必须是一个 `##`。只用短章节名，不带时间、公司、说明文字。按与该岗位的相关度从高到低排列，从以下集合中按原简历实有内容取用，不要新造章节，也不要为空章节保留标题：
`## 个人简介` `## 专业技能` `## 工作经历` `## 实习经历` `## 项目经历` `## 教育背景` `## 荣誉奖项`

三级标题 `###`：**章节内的一个具体条目**——一家公司、一个项目、一所学校。仅出现在经历类章节（工作/实习/项目/教育）之下，`个人简介` `专业技能` `荣誉奖项` 三节内不使用 `###`，直接跟列表。条目标题自身写成一行，时间区间同行右接，用两个空格加 `||` 再加两个空格分隔：
- 工作/实习：`### 公司名  ||  2025.07 – 2025.10`（职位写在下一行的首个要点，或紧跟公司名后用空格分隔）
- 项目：`### 项目名  ||  2025.01 – 2025.05`
- 教育：`### 学校 · 专业 · 学历  ||  2022.09 – 2026.06`
- 时间统一 `YYYY.MM` 加 ` – `（在职/在读写 `至今`）；原简历没有时间就整个 `||` 段省略，不要编造。

正文（各级标题之下）：
- 一律用 `- ` 无序列表承载，不写大段散文；单条不超过两行。
- `## 专业技能` 用 `- **分类**：技能1, 技能2` 的形式分组，分类名如 后端 / 框架 / 数据库 / 中间件 / 工程化 / 其他。
- `###` 项目条目下，先 `**技术栈**：…`，再（可选）`**项目描述**：…`，最后 `- ` 要点列表陈述职责与成果。
- 量化指标与关键技术名用 `**加粗**` 突出，例如 `**提升 28.5%**`、`**Spring Boot**`；加粗只用于此，不要整句加粗。
- 不使用表格、图片、代码块、水平分割线，也不要 HTML 标签——下游要把它渲染成一张图片。