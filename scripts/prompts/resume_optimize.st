请针对以下岗位要求，生成简历优化建议。

原简历:
{resume_text}

目标岗位:
- 公司: {company}
- 职位: {position}
- 薪资: {salary}
- 要求: {requirements}

匹配分析:
- 匹配度: {match_score}
- 缺失项: {missing_items}
- 可优化点: {optimization_points}

请以JSON格式返回（不要包含任何解释文字）:
{{
    "optimization_suggestions": {{
        "must_add": [
            {{"section": "建议添加的位置", "content": "具体内容建议"}}
        ],
        "should_adjust": [
            {{"section": "建议调整的部分", "suggestion": "调整建议"}}
        ],
        "keywords_to_emphasize": ["需强调的关键词"],
        "format_suggestions": ["格式优化建议"]
    }},
    "optimized_resume": "优化后的完整简历（Markdown格式，保持真实性，不编造虚假经历）",
    "key_changes": ["主要修改点列表"]
}}

注意：
1. 不要编造虚假经历，只建议用户补充真实信息
2. 优化应聚焦于突出已有优势
3. 使用STAR法则组织项目描述
4. 不需要返回个人信息：`optimized_resume` 内不得出现姓名、电话、邮箱、教育背景等可定位到个人的信息；首行/首个内容必须是正文章节（如「专业技能」「项目经历」），不得以姓名作为标题或第一行。

`optimized_resume` 的 Markdown 格式要求（严格遵守，标题层级不得错位）：

一级标题 `#`：**整份简历一律不使用**。它在常规简历里是姓名标题，而本文档不含个人信息，因此没有任何内容配得上这一层级。出现 `#` 即视为格式错误。

二级标题 `##`：**章节名**，也是文档的最高层级。首行必须是一个 `##`。只用短章节名，不带时间、公司、说明文字。从以下集合中按原简历实有内容取用：**原简历实有的章节一律保留，不得删除**——尤其「荣誉奖项」「开源经历」是无工作时长的应届生最有力的差异化背书，即使与岗位相关度靠后也必须保留；相关度只决定先后顺序，不决定去留。不要新造章节（原简历没有的），也不要为空章节保留标题。
`## 个人简介` `## 专业技能` `## 工作经历` `## 实习经历` `## 项目经历` `## 教育背景` `## 荣誉奖项` `## 开源经历`

三级标题 `###`：**章节内的一个具体条目**——一家公司、一个项目、一个开源项目、一所学校。仅出现在经历类章节（工作/实习/项目/开源经历/教育）之下，`个人简介` `专业技能` `荣誉奖项` 三节内不使用 `###`，直接跟列表。条目标题自身写成一行，时间区间同行右接，用两个空格加 `||` 再加两个空格分隔：
- 工作/实习：`### 公司名  ||  2025.07 – 2025.10`（职位写在下一行的首个要点，或紧跟公司名后用空格分隔）
- 项目：`### 项目名  ||  2025.01 – 2025.05`
- 开源：`### 项目名  ||  2024.01 – 至今`（GitHub 链接、Star 数等量化背书写在该条目下的要点里；原简历没有时间就把 `||` 段整段省略，不要编造）
- 教育：`### 学校 · 专业 · 学历  ||  2022.09 – 2026.06`
- 时间统一 `YYYY.MM` 加 ` – `（在职/在读写 `至今`）；原简历没有时间就整个 `||` 段省略，不要编造。

正文（各级标题之下）：
- 一律用 `- ` 无序列表承载，不写大段散文；单条不超过两行。
- `## 专业技能` 用 `- **分类**：技能1, 技能2` 的形式分组，分类名如 后端 / 框架 / 数据库 / 中间件 / 工程化 / 其他。
- `###` 项目条目下，元数据一律**单行内联、绝不打点**（不给「技术栈/项目描述/项目地址」加 `- `）：`**技术栈**：技能1, 技能2`、`**项目描述**：一句话概述`、`**项目地址**：<链接>` 各自独占一行；只有**职责与成果**要点才用 `- `。示例：
  ```
  ### 仿华为商城AI客服  ||  2025.01 – 2025.05
  项目地址：http://60.205.230.218:5173/
  **技术架构**：SpringBoot + MyBatis Plus + Milvus + Redis + Redisson + Apache Tika
  项目描述：面向华为商城商品咨询、规格比对、促销政策、售后服务等场景的 AI 智能助手。
  - 基于 LLM 构建意图识别树，Top-1 准确率达 90%+。
  - 基于多路检索引擎实现意图定向 + 全局向量双路召回，Top-1 召回命中率提升 28.5%。
  （上例中「项目地址」「技术架构」「项目描述」三行不打点，`- ` 只用于职责与成果）
  ```
- 量化指标与关键技术名用 `**加粗**` 突出，例如 `**提升 28.5%**`、`**Spring Boot**`；加粗只用于此，不要整句加粗。
- 不使用表格、图片、代码块、水平分割线，也不要 HTML 标签——下游要把它渲染成一张图片。

以下是一份完整的理想输出示例，供参照整体版式（重点：技术栈/项目描述/项目地址等元数据单行内联、不打点；只有职责与成果要点才用 `- `）：

## 实习经历

### 山西物联工业自动化技术研究院有限公司  ||  2025.07 – 2025.10

- 参与校内智能问答系统优化项目，主导检索链路根因分析、参数调优及评测工程化建设。
- 系统性复盘 300 余条低质样本，定位切片过长、阈值偏低等四大主因。
- 构建覆盖 8 类场景的 620+ 标注样本数据集，开发 LLM 自动化评测流水线。

## 荣誉奖项

2024 年中国大学生计算机设计大赛 国家二等奖 · 参赛作品《智能简历解析系统》

2024-2025 年第十三届、第十四届中国软件杯 国家三等奖 · 参赛作品《AI智能教育平台》《AI模拟面试系统》

2024 年“向日葵杯”华北五省大学生计算机应用大赛 一等奖 · 参赛作品《基于知识图谱的诗词学习系统》

2025-2026 年连续两年中国大学生计算机设计大赛 省级二等奖

2026 年于《忻州师范学院学报》参与发表期刊《多模态智能体赋能的大学生线上面试系统构建》

## 开源经历

### boss-crawler-skill  ||  2024.01 – 至今

一套完整的 BOSS 直聘求职自动化工具 skill：爬取岗位 → 解析简历 → 智能匹配 → 可视化报告 → 自动投递。GitHub Star 40+：https://github.com/zhansan379/boss-crawler-skill

## 项目

### 仿华为商城AI客服  ||  2025.01 – 2025.05

项目地址：http://60.205.230.218:5173/

项目描述：面向华为商城商品咨询、规格比对、促销政策、售后服务等场景的 AI 智能助手。参考华为商城真实业务体系（约 300 SKU，覆盖手机、平板、穿戴、智能家居等 6 大品类），梳理 3 个一级意图、22 个二级意图，构建 68 篇知识库文档，设计 220 条评估样本。

**技术架构**：SpringBoot + MyBatis Plus + Milvus + Redis + Redisson + Apache Tika

**核心职责与成果**：

- 基于 LLM 构建意图识别树，覆盖知识库问答、工具调用（MCP）、系统指令意图，低置信度时自动触发澄清反问，意图识别 Top-1 准确率达 90%+。
- 基于多路检索引擎实现意图定向 + 全局向量双路召回，通过多线程并发执行，配合去重与重排序后处理，相对传统 RAG，Top-1 召回命中率提升 28.5%、过度检索率降至 10% 以内，上下文精准度提升 20.5%，平均首字响应 7 秒内。
- 基于 LLM 实现问题重写与子问题拆分，结合多轮对话历史做指代消解与上下文补全，支持复杂问题并行子查询，配合术语归一化与规则兜底，Recall@1 提升 18.8%。
- 设计会话记忆管理机制，采用滑动窗口 + LLM 摘要压缩混合策略，超阈值对话异步自动摘要，以「长期摘要 + 近期完整对话」兼顾长短期记忆，有效控制长对话 Token 成本，摘要持久化至 PostgreSQL。
- 集成 MCP 协议扩展工具调用能力，基于意图识别路由工具，LLM 按工具 Schema 自动提取参数，支持多工具并行调用，打通知识检索与外部业务系统。
- 基于 Redis 信号量 + ZSET + Pub/Sub 实现分布式排队限流，Lua 脚本保证原子性，支持公平排队与超时拒绝，SSE 推送队列状态。自研三态熔断器监控多模型健康度，结合流式首包探测与优先级降级链，模型故障时自动切换备选模型、用户无感，保障多模型混合调度下的高可用。
- 搭建基于 RAGAS 的端到端评测体系，覆盖检索、延迟、回答质量三大维度 20+ 指标，支持 A/B Diff 对比与人工校正，驱动答案忠诚度 +10.3%、答案相关性 +8.0% 等关键指标持续迭代。

## 技能

### AI / LLM 应用开发

- 熟练 Python，熟悉 FastAPI、Django；深入使用 LangChain 与 LangGraph 构建复杂 Agent 及工作流，掌握 Dify 低代码平台与 LangFuse 可观测性工具，具备 LLM 应用全链路开发与调优能力。

### 通用全栈工程基础

- 具备扎实的 Java、SpringBoot/Spring Cloud 微服务、MyBatis/MyBatis Plus、MySQL 索引与事务优化、Redis 数据结构与策略，以及 Vue2/Vue3 前端（含 Element/Vant）开发能力；熟悉 Git、Maven、Docker 基础操作，并熟练使用 Claude Code、Cursor、GitHub Copilot 等 AI 辅助工具。