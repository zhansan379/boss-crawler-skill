你的任务：判断一份投递材料（优化后的简历，或打招呼消息）里的技术词，在原简历里到底有没有真实依据。

## 背景

投递材料由大模型从原简历改写生成。它只应包含**原简历里真有依据**的技术词 ——
不允许编造简历里没有的技能。下面是**完整**的原简历原文和**完整**的待查材料：
不依赖任何别人预先筛好的词表，由你**通读全文**自己判断。

原简历：
{resume_text}

材料类别：{material_kind}
待查材料：
{material}

## 你的任务

1. **通读完整材料**，识别出其中出现的**所有技术名词 / 工具 / 平台 / 方法论名词**。
   多词复合名词（如 `Vibe Coding`、`GitHub Copilot`、`React Native`、`Claude Code`、
   `Machine Learning`）必须当作**一个整体**识别和判断，不许拆成单个单词。
2. 逐一判断每个技术词在原简历里**有没有真实出处**。
3. 输出你识别出的**全部**技术词，每个都给出判断 —— 不只报"有问题"的那些。
   这样"我认为整份材料干净"和"我其实没仔细看"能被区分开。

## 判别规则

一个词算「有依据」，当且仅当它在原简历里能找到真实出处，包括但不限于：
- 原简历出现这个词本身（哪怕经大小写、连字符、中英文括号变体）；
- 常见缩写 / 别名：原简历写「深度学习」「卷积神经网络」，材料写 DL / CNN 算有依据；
  原简历写 REST，材料写 RESTful 算有依据；原简历写「自然语言处理」，材料写 NLP 算有依据；
- 中英等价：原简历用中文表述某项技术，材料用了它的英文名或缩写且确实指同一项；
- 同一技术生态下由某个已写明的表述**直接且必然**推出的直接子项（如「微信小程序」
  生态里的「云开发」，原简历明确写了主技术栈）。

一个词「没有依据」，只要原简历里真的找不到它的出处 —— 它是某个技术/工具/平台的
说法，而原简历从未提及、也无法合理地由任一已有表述直接推出。

## 判定倾向（最重要）

这是投递前最后一道防编造闸门。漏掉一个真编造，人会带着假技能去面试，代价不可逆。
所以：
- **拿不准的一律判「没有依据」**，不要靠猜去帮材料开脱。
- 只有你能在原简历里**明确指认出处**时才判「有依据」，并且**必须**把它写进 reason
  作引证；判「有依据」却没有引证的空理由，会被当作无效，按「没有依据」处理。
- 宁可多报一个句子让用户扫一眼放行，也不放过一个真编造。

## 输出

只输出下面这个 JSON，不要任何解释文字、不要 Markdown 代码块围栏、不要在前后加内容：

{{"terms": [
  {{"term": "技术词原样，复合名词写完整不拆开", "supported": true, "reason": "原简历里的引证原文，说明它确实有出处"}},
  {{"term": "技术词原样", "supported": false, "reason": "缺依据的说明，或写「无」"}}
]}}

要求：
- `terms` 必须包含你识别出的**全部**技术词，每个都带 `supported` 和 `reason`。
- `term` 写它在材料里的原样写法，多词复合名词写完整（不要拆成 `GitHub` 和 `Copilot` 两条）。
- reason 是给用户核对的依据，尽量具体；`supported: false` 的也写一句为什么缺依据。